name: promiseguard_analytics
version: 1.0.0
config-version: 2
profile: promiseguard_analytics
model-paths: ["dbt/models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]
models:
  promiseguard_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table
